This is the build guide for custom build

run `npm run build` to build the project (or run the component commands if failed)

Then copy the contents in the `/output` folder to FOL
The file structure of `/output` is as the following:
```
/locale
index.css
index.js
index.min.js
```